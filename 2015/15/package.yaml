name:                solution

dependencies:
  - base >= 4.7 && < 5
  - matrix
  - vector
  - composition
  - au

default-extensions:
  - ParallelListComp
  - Strict

executables:
  solution:
    source-dirs:      src
    main:             Main.hs
