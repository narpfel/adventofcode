name:                solution

dependencies:
  - base >= 4.7 && < 5
  - containers
  - au

default-extensions:
  - LambdaCase

executables:
  solution:
    source-dirs:      src
    main:             Main.hs
