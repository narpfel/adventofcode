name: solution

dependencies:
  - base >= 4.7 && < 5
  - containers
  - split
  - au

default-extensions:
  - LambdaCase
  - ParallelListComp
  - DeriveFunctor

ghc-options:
  -Wall
  -Wextra
  -O2

executables:
  solution:
    source-dirs:      src
    main:             Main.hs
